import classes from './Header.module.css';

const Header = () => {
    return (
        <header>
            <h1 className={classes.center}>Concular Market Place</h1>
        </header>
    );
}

export default Header;